extends layout.pug

block content

    h1 Delete Book
    form(action='/books', method='POST')
    h2 Are you sure you want to delete #{book.title}?
    p
      input(type='submit' value='Delete Book')
    p
      a.button(href='/') Cancel
